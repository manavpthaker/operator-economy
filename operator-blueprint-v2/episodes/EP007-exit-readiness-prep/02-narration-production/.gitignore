# Audio is episode media, not repository content.
# The episode workspace rules state that raw/, selects/, pickups/ and master/
# are locations, not permission to commit audio. Large or private media follows
# the repository's storage policy.
raw/
selects/
pickups/
master/
*.wav
*.pcm
*.mp3
