# Gate V2 episode engine draft candidate: EP009

## Status

**UNGATED DRAFT. V2 HAS NOT BEEN ATTEMPTED OR PASSED.**

V1 is not passed, and proposed Step 3 v0.3 has not regained owner authority. This is review material only: it contains no owner approval, activates no gate, and authorizes no later production step.

| Artifact | SHA-256 |
|---|---|
| `engine.json` | `f3c9a88d683dd7a99fe5dbd1239611fac723ab8b726d4c0a7b259733c4db6373` |
| `V1-INPUT-LOCK.md` | `fa71e0b0b7a3ea05bba3815184975acaa4268c78ff6079a9f988689ae85d014f` |
| Proposed process manifest | `4f830d71d677efa8915429dfe35628012289c28ac988cb74f77261331519690c` |

## Candidate content

- Seven business fields retain exact Canvas provenance.
- Five business operations are `DERIVED`, each with current source path, hash, locator, and exact Markdown quote.
- Three `AUTHORED` establishment rows reveal viewer context only: buyer/beneficiary, current platform route/relay, and audit objects/test scope.
- Four upstream changes are excluded behind explicit Boundary Ledger design-system blockers. Eight excluded outcomes or unsupported changes are recorded in total.
- The platform route, property route, relay, separately consented property record, audit inputs, ceiling, direct-stack cost, recommendation, post-stay work, scorecard, and diagnosis keep distinct identities.

## Exact operation audit

| Row | Selected role / operation | Required state change represented | Semantic ruling |
|---|---|---|---|
| `BO-003` | `activeCommitment` / `correct` | A recorded findability finding stays visible while an in-scope revision inside the property's account replaces its consequence. | Fits. The row is limited to the Canvas's explicit findability fix. Testing and creation of the prior finding are not smuggled into `correct`; DSB-004 blocks them. |
| `BO-005` | `externalDependency` / `route` | An existing checkout-linked work item moves from a consented queue into drafted and scheduled material awaiting review. | Fits. It creates neither the guest record nor human approval, and it encodes no send. |
| `BO-006` | `accountableEvidence` / `pin` | Current direct-share evidence gains visible baseline context without either source record changing. | Fits. It does not calculate the ceiling or create the scorecard. |
| `BO-008` | `activeCommitment` / `correct` | A visible no-movement mark remains unchanged while a bounded diagnosis replaces its undecided operating consequence. | Fits. The world makes the no-movement mark visible before this correction. |
| `BO-009` | `activeCommitment` / `interrupt` | Revocation makes owned-contact eligibility and pending post-stay work unavailable. | Fits. The closed transition is non-reversible in this draft because Step 1 supplies no re-consent transition. |

All five role/operation pairs are permitted by the pinned provisional motion binding. That mechanical permission is necessary but not sufficient; the state-change audit above is the controlling semantic check.

## Open design-system blockers

1. `DSB-001`: no pinned operation creates an additional permissioned property-held record while leaving the platform relay untouched. `return` would wrongly imply transfer from rented to owned.
2. `DSB-002`: no pinned operation makes and records the human recommendation `retainer`, `audit-only`, or `not this property`. `settle` only holds a state already changed.
3. `DSB-003`: no pinned operation computes the commission line, baseline, and ceiling from inputs or binds the calculated result into the audit bundle. `pin` attaches evidence; it does not calculate.
4. `DSB-004`: no pinned operation tests the direct path and creates an attributable finding/fix list. `correct` requires the prior finding to exist and remain legible.

The engine excludes those four changes rather than reverse-fitting nearby verbs. Resolution requires an approved Boundary Ledger change or a changed upstream business state, followed by repinning both Boundary Ledger hashes and re-reviewing the exact engine.

## Evidence and outcome boundary

The engine may not turn a direct booking, commission saving, direct-share increase, owner approval, recommendation branch, renewal, six retained properties, or a viable room band into a delivered result. The direct-stack outflow must remain visible whenever gross avoided commission is translated into modeled net recoverable value. The platform remains a useful paid route, not a villain or an address to transfer.

## Mechanical result

Command:

```text
python3 operator-blueprint-v2/03-visual-translation/fixtures/validate.py --through V2 operator-blueprint-v2/episodes/EP009-direct-booking-recovery/03-visual-translation
```

Result on 2026-09-04:

```text
gates failing: none | expected: none | PASS
```

This is structural hygiene only. The validator does not decide whether an operation's prose satisfies its exact `requiredStateChange`; inspect V1's Markdown decision; verify the Step 1 internal dependency chain or script status; enforce the literal E6 phrase; test the separate generic narration tools; or supply process/owner authority. A clean result cannot pass V1 or activate V2.

## Carried narration and reproducibility caveats

- Locked working master: `e433c0fd6d7dd522efb9f6593986f930f9ccc54b2be5132dec50c20ff3c1f944`, 1233.602 recorded seconds.
- c05 remains marginal at tail energy `0.024`, accepted at N6/N7 with a full-length final word and matching transfer length.
- Independent second-person listening was not performed, as disclosed in the lock.
- The generic transcript/state validators reject the exact locked bespoke schema and 16-bit frozen master. The package verifier passes. The proposed V1 criteria do not require those generic checks, so this is a separate process/tooling decision rather than a Step 2 change request or independent V1 blocker.
- The master is present and hash-verifiable on this host but ignored and absent from `HEAD`; another host needs a separately transferred, hash-matched media package.

## Decision

Result: **NO GATE DECISION — UNGATED DRAFT; SEMANTIC REVIEW BLOCKED BY DSB-001 THROUGH DSB-004**

Approved by: **nobody**

Formal review requires, in order: a valid V1 disposition, authority for the exact Step 3 process snapshot, resolution or explicit upstream disposition of the four Boundary Ledger gaps, then named owner review of this exact engine hash.
