# Gate V2 candidate: episode engine — EP008

## Status

**UNGATED REPAIRED DRAFT. V2 HAS NOT BEEN ATTEMPTED OR PASSED.** V1 is returned, process
authority is absent, and nine engine semantics remain blocked. This file records no approval.

| Artifact | SHA-256 |
|---|---|
| `engine.json` | `314c1a0d185e67c56cbbd6bbdef16f6cafd9fdcb9406df410ad0ab2ed9fef7c0` |

V1 is **RETURNED**: the literal `Gate E6: PASSED` phrase is absent, the Step 1 internal hash/status
chain is stale, and Step 3 v0.3 remains proposed with authority not re-granted. A clean Step 3
validator result cannot activate this gate. The separate generic narration-tool mismatch is recorded
for process design, but the proposed V1 criteria do not make those generic checks a gate condition.

## Repaired candidate model

The engine contains 21 derived operations:

`BO-001`–`BO-009`, `BO-011`–`BO-013`, `BO-015`–`BO-017`, and `BO-019`–`BO-024`.

Material corrections in this candidate:

- `BO-011` starts from unknown wizard residue and interrupts only after a recorded no-human-work
  result. It no longer asserts an open service gap.
- `BO-022`–`BO-024` preserve separate recurring order-status and broken-item-refund examples and
  mark their ending states illustrative, not customer evidence.
- Gorgias and Zendesk, economic buyer / brand payer / founder-authorizer, and each money action are
  separate identities.
- `BO-006` safety narrowing is non-reversible in the world absent a separately sourced
  reauthorization operation.
- `BO-005` only pins a checked playground result. `BO-016` only revises the identified guidance
  rule. A later attempt requires a separate `BO-005`; neither operation opens eligibility.
- `BO-012` only routes existing scoped client-controlled admin control into temporary custody. It
  does not create consent.
- `BO-017` only pins an observed sandbox-run result. It does not perform the test.
- Former `BO-010` is withdrawn: `settle` cannot create report delivery or signature.

Every retained business-operation row is `DERIVED`, cites an exact locked Markdown path, SHA,
heading, and verbatim quote, and selects a role/operation pair permitted by the pinned provisional
motion binding. The establishment rows remain `AUTHORED` viewer-knowledge changes only.

## Open Boundary Ledger gaps

`engine.json` records nine fail-closed blockers with no local substitute:

1. `DSB-001`: configuration rollback after a handover storm.
2. `DSB-002`: report delivery and founder signature.
3. `DSB-003`: creation and acceptance of the expand / hold / turn-off recommendation.
4. `DSB-004`: paid-audit payment and later credit.
5. `DSB-005`: creation of written ticket-reading consent.
6. `DSB-006`: actions-register creation and per-action authorization.
7. `DSB-007`: opening go-live eligibility after a pass.
8. `DSB-008`: executing the sandbox test rather than attaching its observed result.
9. `DSB-009`: creating the first-draft intent map and never list rather than routing existing
   ticket-intent objects.

The corresponding state changes are in `excluded_operations`; none is forced into `route`, `pin`,
`correct`, `return`, or `settle` for coverage.

## Mechanical result, not gate status

```text
python3 operator-blueprint-v2/03-visual-translation/fixtures/validate.py --through V2 \
  operator-blueprint-v2/episodes/EP008-ai-implementation-service/03-visual-translation

03-visual-translation
   -> gates failing: none | expected: none | PASS
```

This proves current Step 3 schema, provenance, and Boundary Ledger binding hygiene only. It does not
cure V1, resolve the separate narration-tooling decision or nine semantic gaps, approve v0.3, record
an owner decision, or authorize V3 or downstream work.

Result: **UNGATED REPAIRED DRAFT — NOT APPROVED**

Approved by: **nobody**
