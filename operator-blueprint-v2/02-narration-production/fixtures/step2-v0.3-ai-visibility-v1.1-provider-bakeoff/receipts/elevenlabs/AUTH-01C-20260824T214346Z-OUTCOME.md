# AUTH-01C Named-Sample Batch Outcome

Status: `FAILED_CLOSED_ON_FIRST_SAMPLE_SIZE_IDENTITY_MISMATCH`

## Authorized boundary

AUTH-01C authorized exactly three named ElevenLabs sample-audio `GET` requests, at most three
downloads, 20,000,000 total bytes, and zero spend. It permitted no metadata request, discovery,
selection, retry, redirect, upload, TTS, voice mutation, Hume disclosure, or clone creation.

The authorization was committed before execution and consumed before the first network request.

## Execution result

The first bound endpoint returned `audio/mpeg`, but the exact response was 8,641,768 bytes while
the hash-bound AUTH-01B inventory recorded 5,760,813 bytes for that sample. The difference was
2,880,955 bytes, about 50 percent above the inventory value. The runner preserved the exact returned
bytes as blocked local evidence, wrote the per-sample and batch failure receipts, stopped, and made
no request for samples two or three.

| Check | Result |
| --- | --- |
| Authorization consumed before network | pass |
| Provider calls | 1 of 3 maximum |
| Metadata calls | 0 |
| Downloads attempted | 1 of 3 maximum |
| Downloads completed as eligible source samples | 0 |
| Bytes received | 8,641,768 of 20,000,000 maximum |
| Redirects followed | 0 |
| Retries attempted | 0 |
| Sample two requested | no |
| Sample three requested | no |
| Spend | $0 |
| Stop reason | `sample_size_identity_mismatch` |

## Preserved first response

- Sample ID: `rHrnt10vbIpD444OcGVZ`
- Inventory filename: `ivc_1.mp3`
- Local status: blocked evidence only
- SHA-256: `dd3f0887acb5bc4c623476eb053136d3f0ce7d6828168874911f8b0dcecd64f9`
- Locally calculated MD5: `8a0b915adcb970b320f409549a8e9ff1`
- Inventory provider hash: `2cf6f7f07cdb931a5cf93490a1b7ebdb`

ElevenLabs exposes the 32-character inventory `hash` but does not document its algorithm. The local
MD5 does not match that value. This is additional identity uncertainty, not proof that the provider
field is MD5 or an independently enforceable checksum.

Read-only local media inspection found one decodable MP3 stream: 44.1 kHz, mono, 192 kbps, and
360.000 seconds. A full decode completed without an ffmpeg error. The decoded peak measured about
-2.52 dBFS and RMS about -20.47 dBFS. No silence of at least 1.5 seconds below -45 dBFS was detected.
Those checks establish only that the exact response is technically reviewable.

## Authority and provenance result

The preserved file is not an approved clone source, production source, or narration asset. Technical
inspection cannot confirm that it is Manav, wholly human, single-speaker, original, or suitable for
cloning. The owner may listen to it as blocked evidence. AUTH-01C is consumed and cannot be resumed
or retried. Retrieving either remaining sample requires a new owner decision and a new, separately
bounded authorization. Hume remains untouched and blocked.
