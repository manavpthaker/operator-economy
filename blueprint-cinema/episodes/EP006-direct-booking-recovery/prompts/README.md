# EP006 prompt packets

`generated/director/` contains deterministic directing context compiled from the locked VO, approved visual plan, persistent world, evidence, and tickets.

`generated/build/` remains empty until an authored `scene-directions.json` validates. A build packet is an implementation recipe, not approval or permission to generate media.

The first bounded directing target is `sequence-hook-01`. Do not use the rejected map-camera or deck prototypes as visual inputs.
