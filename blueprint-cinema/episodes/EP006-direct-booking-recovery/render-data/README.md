# Render data

This folder will contain deterministic renderer props compiled only from hash-current, validated, approved greenfield artifacts.

Generated render data must include provenance back to the exact input lock, engine, world, and visual-plan hashes. It must never consume legacy `render_data/blueprint.json` or infer scene layouts from narration.

No render data has been generated here.
