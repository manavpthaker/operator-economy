# R32 generated-performance QA preparation

2026-09-11. **Preparation is complete; actual new-output QA has not run.**

All six issued input hashes match. [qa_outputs.py](qa_outputs.py) and the [integration checklist](integration-checklist.md) are ready. Preparation checked the installed FFmpeg/FFprobe and exact original reference audio: 15 seconds for post-title and 12.041667 seconds for the question. Evidence is in [preparation-checks.json](preparation-checks.json).

The script requires an explicit local path and SHA-256 for each new output. It performs probe/decode, frame cadence/repeat checks, and dense performance/face contacts. For restored avatars, it measures new waveform insertion and independent early/middle/late windows against the pinned original audio; it flags insufficient picture coverage and never assumes R31's offset. Native speech is kept separate from final synchronization evidence. No waveform result becomes a perceptual lip-sync verdict.

The checklist covers near-lens attention through “one number,” a developing pensive expression at the question turn, hand continuity, ending mouth rest, unchanged original narration and the actual integration seams. The issued request bundle contains only the two avatars, so buyer-specific performance review awaits root's exact direction/reference and output pins.

No generated output was opened, no service was contacted, and no runtime, provider, history, canonical or approval file was changed. Next action: receive the exact output pins, then run and visually review the bounded QA outputs. Owner acceptance remains separate.
