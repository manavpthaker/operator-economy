# R38 built candidate: bounded semantic and visual QA

Read-only review of the current `owner-dependency.html`, `CONTACT-V2.jpg`, and the authored bench, routine, intake and output SVGs. This is not a full audiovisual or encoded-frame review. The contact sheet predates the root-reported footer correction; that corrected footer was not independently rendered here.

The build covers the requested direction: the repeated zoom-out is removed, the presenter slot is explicitly marked as pending, the graphic begins with the heading alone, small business/job illustrations arrive separately, and the operating model accumulates piece by piece. The first current-work item passes before the owner-removal test; a second passes after she withdraws. The retained worker prevents the image from implying that transferability requires an unstaffed or fully automated business.

The alternative begins with both a changed question and “If it all depends on her…” before know-how changes custody. The workshop, worker, bench and past revenue remain. The subsequent request stops at the missing routine. Those are coherent conditional states; I found no additional material claim that this seller has actually passed or failed the test.

## One material readability fix

The book is the essential connection, but `r38-routine.svg` uses `fill="none"` throughout. In the supplied contact sheet, bench and belt lines run through the pages, making the book resemble extra bench geometry. The underlying bench also appears as several stacked surfaces, so the transparent book is especially easy to lose.

Give the book's outer silhouette a paper fill (`#F5F0E6`) beneath its existing authored strokes. Keep the interior page lines unfilled. That preserves its outline and makes the same book readable both on the bench and against the owner's torso. This is a local readability fix; it does not change the sequence or claim.

A later encoded frame should verify that the book is recognizable while retained, while carried by the owner, and immediately before the stopped task occupies its former position. If that object cannot be tracked, the animation's main causal explanation remains unclear even though the timeline is structurally correct.

The unchanged spoken callback and still presenter remain explicitly pending. This review does not call those owner requests complete. No runtime edits, source changes, provider calls or approval actions were performed.
