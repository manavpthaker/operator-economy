# EP007 R79 owner-locked conform reference

R79 is frozen as the exact owner-approved whole-episode picture/audio reference, including the three
disclosed inherited R39/R47 exceptions. Its picture decisions, cuts, retained takes, narration timing
and ending must not change unless the owner explicitly reopens R79.

This is a **pre-Resolve reference package**, not the canonical Blueprint Cinema picture lock. Under
`blueprint-cinema/TOOLCHAIN.md`, canonical picture lock exists only when the Blueprint Cinema edit
manifest, Resolve timeline, HyperFrames plate manifest and approved reference agree. Those Resolve
and interchange artifacts do not exist yet.

`manifest.json` binds the exact owner event, accepted reference bytes, PCM, timing map, current and
historical verification, build provenance, exception dispositions, missing package components and
the required downstream sequence.

The MP4 and PCM are pointers to ignored local media, not durable copies inside this handoff. The
package is therefore not ready to transfer to another machine until those assets are placed in an
approved durable store or copied into a conform package with their hashes rechecked.

