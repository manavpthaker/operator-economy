# Current r6 software-demo source record

`SELECTIONS-r2.json` is the only selected source record for the r6 private review candidate. It binds F03 as illustrative establishing film for output frames **[16704,16752)** and the real, fictional Node-RED/OpenAI workflow capture for **[16752,17458)**.

The r2 capture records under `capture/node-red-openai-real-run-r2/` are the selected evidence package. They bind the raw capture, safe persisted-state evidence, declared raw-time ellipses, crop, final screen source, screen review, private-route check, and r6 verification event.

Earlier r1 selection and capture artifacts are retained as superseded working history. They are not selected for r6, must not be used to rebuild r6, and do not establish the final screen-source binding.
