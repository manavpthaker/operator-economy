# R18 browser review

Status: review candidate, not creative acceptance or encoded delivery.

- Player: http://100.101.49.30:3034/ (Tailscale connected, host Mac awake).
- Studio: http://localhost:3006/#project/r18-business-dependence
- Changed interval: 30.208333–35.083333 in the retained 71.5-second sequence.

The orders-only model has been replaced by Customers, Decisions and Operations converging through the owner toward the team. “What depends on her?” frames the first state as a test. At the existing cue the owner and her central connections disappear; all three areas and the team remain. “Absent for a month” and “What still works without her?” land the unresolved question.

Verification:

- `VERIFICATION.json` passes: 27 R17 runtime source files unchanged; 25 retained files identical; all 18 clip declarations preserved except graphic identity. Original audio assets and placements remain identical. Duration remains 1,716 frames at 24 fps.
- `.hyperframes/check-final.json` passes strict lint, runtime and sampled layout checks with zero findings; sampled contrast passes 18/18. Motion assertions were disabled.
- Final first/action/exit snapshots are in `snapshots/contact-sheet.jpg`. Root and independent reviewer found the categories, owner junction and missing connections legible, with no blocking visual issue for this rough preview.
- Live player inspected at default desktop size and at 844 × 390 landscape. Both headline states, complete connection through the owner, missing junction and retained team/areas were visibly confirmed. Temporary viewport override was reset. Playback advanced through the changed passage with narration enabled; this is not a claim of a complete listening review or proven first-view comprehension.
- Player opened in the Codex in-app browser and marked as a deliverable. Physical phone/Safari and final compression QA remain unperformed.
- `.hyperframes/phone-player.json` records the 20 staged runtime files; each matched final source bytes and returned HTTP 200 over Tailscale. The stage contains runtime files only.
- Final animation map generated: 69/96 non-micro tweens, 6 paced-fast and 67 collision flags. Child/mounted duplicate entries and shared junction traces are present; video intervals and holds are reported as dead zones. This is diagnostic output, not a clean motion verdict.
- Focused `keyframes --shot` could not resolve the path targets inside the shared trace helper. A generic path diagnostic rendered the wrong subject (the Customers tab) and is not used as proof. Full-frame time samples and live player states are the visual evidence for the scene.

The remaining creative test is whether the viewer registers the connected state before the owner disappears within the existing 4.875-second slot.

No paid generation, final export, gate update, commit, push or publication.
