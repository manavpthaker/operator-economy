# R78 — mobile framing for the real-tool inserts

Use the same R77 actual-capture frame selections. Crop the recorded pixels to the active prompt/result or evidence table, and make only a quiet, seekable drift that retains the genuine cursor. Do not recreate the interface, overlay a substitute cursor, invent a click, change the narration, or make the labelled fictional example look like a client result.

S15 should settle on the drafted authority statement and then the returned **“Exception pricing: unresolved”** result as the real pointer crosses the response. S16 should eliminate the empty Sheets columns and keep the unresolved exception, source package, and Q3 implication readable on a phone. The paper frame and the **“Illustrative example · fictional business”** label remain.
