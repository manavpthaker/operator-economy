# S21 verification corrections

The research-to-fixed-deadline transition originally faded text at overlapping coordinates. Replace the departing research layer instantly at the cue; retain the exit calendar. The animation-map pass also identified the outgoing fee label crossing the incoming research label. Both parent transitions now hard-hide the outgoing groups before revealing the incoming groups; the persistent heading and retained calendar prevent a blank field. The final tests transition uses the same deterministic parent removal.

The horizontal path's animation-map “degenerate” flag describes its zero-height geometry, not an invisible render: the measured 483px stroke is visible and draws between the fee sheet and undated calendar. Reading holds are deliberate under the original narration; the longest seven-second hold retains the qualified evidence and risk argument before the weaker-than-a-deadline judgment. No ambient movement was added.

HyperFrames Studio assigned editor IDs to the HTML after the initial static review. Final check, render and audio/frame verification are bound to the resulting source, including these metadata and transition corrections. The independent three-still review is limited to the earlier source recorded in its manifest; root checks the final encoded output.
