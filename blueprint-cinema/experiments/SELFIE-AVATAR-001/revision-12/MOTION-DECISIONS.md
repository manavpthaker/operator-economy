# Motion decisions

The content job is a reply invitation after an accepted selfie talk. Counterproof Rev E carries the closing, with the viewer's business threshold left open. A 3.5-second hold adds reading time without extending the spoken performance.

Registry discovery: HyperFrames 0.8.36 catalog query `text slide reveal` returned `staggered-fade-up` and `line-by-line-slide`. Installed and inspected `staggered-fade-up`; adapted its paused from/to entrance into a single author-signature settle. Removed the stock word stagger, blur and scale, because the approved Counterproof reading rhythm calls for immediately legible copy. The 80px Countershift is custom geometry for this specific open question, rather than a generic line entrance.

The question and author use the exact locked Archivo file. The off-white field and source-ink question use live Rev E tokens. No institutional OE styling is changed.

Visual review before render: three HyperFrames snapshots at 0, 1.75 and 3.395 seconds show legible question, correct horizontal displacement, clear author signature and consistent margins. CLI check passed runtime, layout, motion and all 18 contrast samples with zero warnings. Keyframes confirms #business moves x0 to x80 at 0.35–1.15 seconds. Final encoded pixels and full-video preservation are verified separately in ASSEMBLY-QA.json.

The owner accepted R11; R12's closing screen remains a review candidate. No global design-lock change or publication approval is inferred.
