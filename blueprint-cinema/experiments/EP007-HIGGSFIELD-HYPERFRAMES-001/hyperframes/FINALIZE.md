# Final validation and preview

Both clips are present. P's exact-audio fidelity failed; the diagnostic preserves its complete145frames and own audio. C's source0.000–6.916667 was selected after sequential frame review, with owner playback still pending. No provider call, upload, render, publication or feedback command belongs to this workflow.

1. Preserve the downloaded originals and provider receipts outside this project as the parent's raw record. Stage C at `public/media/higgsfield-c-owner-response.mp4` and P at `public/media/higgsfield-p-presenter.mp4` only after confirming they correspond to the intended jobs.
2. C source-in0.000 preserves readiness and moves the eye drop across the narrated stop cue. Its source hash and observed timing are recorded in `EDIT-DECISIONS.json`. The mouth closes roughly0.257seconds before “And then”; this limitation is retained honestly. Source review is not owner creative approval.
3. P is already selected only as a raw failed-test diagnostic: source0, complete6.041667-second picture and its own5.952-second returned sound. The specialist found nonconstant offsets exceeding230ms; constant dubbing cannot solve that. Keep `public/audio/presenter-exact.wav` as reference-only. Do not dub, revoice or time-warp P. Preserve the failure label. The preview gate must permit this reviewed failure to be shown rather than conceal it.
4. Run `python3 verify.py --decode`. This checks original/reference WAV PCM, retained media hashes, P's reviewed source hash and raw-sound policy, source coverage, full video decode, contiguous975frame/24fps coverage and byte-identical model. Read `reports/verification.json`. Mechanical errors/missing files must be empty; the recorded presenter fidelity FAIL remains an honest diagnostic finding.
5. Run `npm run check > reports/check-console.txt`. This invokes the pinned HyperFrames 0.8.31 check with snapshots at every seam, all seven scene interiors, model action and last included frame. The `index.motion.json` assertions cover model arrival, ticket arrival and in-frame geometry; film and causal holds deliberately have no continuous-motion assertion. Inspect the written screenshots and any finding crops. Do not waive a black plate, missing mount, label overlap or illegible model line because source hashes passed.
6. After the check passes and source selection is recorded, run `npm run preview`. Its local verification requires both C/P selections to be reviewed before launching Studio with requested port3020. The current returned and verified server is3022. Confirm the actual returned Studio URL and HTTP response; if the port is occupied, use another free port in this project's command and report it. Do not stop another task's server.
7. Do not infer Studio correctness from `check` or snapshots: this exact project passed those while a static separator obscured all live shots. Explicit GSAP gates now act on non-clip wrappers, leaving framework clip ownership intact. Confirm C at13s, the model at28.5s, P at37s and end→exact0 backward seeking in the actual Studio canvas. At exact0 the opening A and existing-Kling label must remain visible; a later positive time is not sufficient proof. Inspect the opening, each cut, C's interrupted answer and protected pause, D's unresolved gaze, the Working Model's dependency/ticket stop, explicit one-second silent break and complete presenter passage with sound. Seek backward across cuts as well as playing forward. Distinguish visual performance judgment from runtime checks. Return the actual Studio URL to the parent for owner review.

No claim of final delivery or owner approval follows from this sequence. Final rendering remains outside this worker's authorization.
