# Plate and alpha behavior

- Current output: one opaque H.264 directed-animatic reference with mixed synthetic VO.
- Current alpha output: none.
- Current separate plates: none.
- Future opaque graphics: place on `V3`, interpret as Rec.709 unless an output manifest says otherwise.
- Future alpha graphics: place on `V4`; the output manifest must declare straight or premultiplied alpha, codec, bit depth, color tags, frame count, source time, handles, and checksum.
- Future evidence: place on `V5`; preserve source context, warning/attribution, crop record, and provenance.
- Future reality: use approved ticketed media on `V1`/`V2`; do not bake an attractive replacement into the system plate.

Classification: `documentation-only`. No alpha or separate plate behavior has been rendered or validated in Resolve.
