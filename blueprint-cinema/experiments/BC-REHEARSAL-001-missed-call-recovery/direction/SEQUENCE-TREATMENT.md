# Sequence Treatment · Missed-call recovery

## Promise

One unanswered call can remain recoverable when the operator preserves the signal, routes it quickly, and leaves uncertain cases behind a human gate.

## Audience inference

The viewer begins with a concrete service event, watches the opportunity disappear into ordinary voicemail handling, sees a bounded counter-system built from the same tag, and finishes with a measurable scheduled appointment—without being told that every call should become a sales sequence.

## Persistent visual spine

The red `CALL-R01 / 20:47` tag is the film's subject. The empty `WO-R01` card is its consequence. Surrounding worlds may cut; these two objects never change identity. The tag can be lost, held, stopped, or attached. The card can be empty, offered, scheduled, or measured. No other object competes for primary focus.

## Seven-shot treatment

1. **After-hours signal · 0.000–13.640.** A neutral home/phone placeholder grounds 8:47 p.m. One ring pulls a red tag from the phone. No one answers; the card at frame edge stays empty. Permission attaches to the tag while `NO GUARANTEE` remains outside it.
2. **Voicemail leak · 13.640–26.370.** Morning arrives by cut. The same tag falls behind paper voicemail sheets. The homeowner's continuation arrow leaves right. Below, the bridge between `ATTENTION` and `ACTION` separates with the tag stranded on the first side.
3. **Route construction · 26.370–34.200.** The tag crosses the only designed handoff into the navy rail. A labeled synthetic CSV opens at the proof bench. `20:47` is highlighted, extracted, and attached to the tag, which then clears capture, classify, and acknowledgment.
4. **Human gate · 34.200–39.880.** `UNCERTAIN` reaches a physical barrier. The operator-hand placeholder blocks the route, turns the tag, reads its context, and deliberately releases it. Only then does a neutral next-slot tab extend.
5. **Stop or book · 39.880–47.680.** One fork demonstrates both conditions without duplicating the tag. The decline rail lights and terminates at `STOP`; the card stays empty. The accepted rail then carries the actual tag to the card, which stamps `SCHEDULED` and gains a measurable registration mark.
6. **Blueprint principle · 47.680–56.050.** A controlled pullback reveals only the causal recovery rail. An `AUTOMATE ALL` bypass is physically removed by the operator hand. `PRESERVE`, `ROUTE`, and `JUDGMENT` register on the objects they describe, then everything settles.
7. **One-week measure · 56.050–61.411.** At the proof bench, seven day ticks span a bracket from the original call tag to reply and booked appointment. The synthetic source stays pinned; no success rate appears. The finished loop holds through the final word.

## Designed-transition budget

One: object-preserving tag handoff at 26.37 seconds. All other changes are cuts. No transition is permitted to obscure the tag, source warning, gate state, or work-order state.

## Finish restraint

The review animatic uses local VO, flat vector geometry, local fonts, and conspicuous ticket slates. No music, polished footage, final evidence, generated interface, stock, color grade, or decorative transition is introduced. It should read like a film with missing plates, not like a designed presentation.
