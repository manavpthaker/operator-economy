---
mode: autonomous
flow: automation
storyboard: no
board_usage: production document only; no live approval gate
---

# Storyboard — The Return Loop

The user supplied the treatment and explicitly authorized the bounded internal render, so this file records the production board without creating a new pause or approval gate.

## Scene 1 — Return to the opening

`0.000–4.375` · absolute `849.517–853.892`

A locked observational plate of the same modest inn desk. A brass key tag sits between the guest’s left-entering hand and the operator’s right-receiving hand. The physical handoff is the action. `FIRST STAY / OTA` lands as a small documentary label; there is no platform logo or booking artifact. At the final moment, a hairline contour isolates the tag for the match.

Snapshot times: entry `0.200`, action `3.700`, exit `4.300`.

## Scene 2 — Permission changes the object

`4.375–12.240` · absolute `853.892–861.757`

The key-tag contour crosses the cut at the same size and screen position. It advances toward a visibly closed `PERMISSION` gate and stops. `FIRST STAY / OTA` remains long enough to cover the final OTA words. At word 1796, the gate resolves; only after that does the tag compress into a paper-and-brass `STAY CONTEXT` object. No outbound route exists yet.

Snapshot times relative to scene: entry `0.150`, action `4.450`, exit `7.700`.

## Scene 3 — Qualification and human judgment

`12.240–20.340` · absolute `861.757–869.857`

The permissioned context enters one decision at a time. Relevance/timing resolves first. A separate sensitive token forks downward and hits a brick-red suppression stop; it remains visibly stopped. Hard cut to the operator at the same inn, making a deliberate physical judgment with the same tag present. The `HUMAN REVIEW` gate resolves only when the operator’s hand completes the action. Hard cut back: the outgoing route opens on the word `direct`.

Snapshot times relative to scene: entry `0.150`, action `5.450`, exit `7.950`.

## Scene 4 — The direct return

`20.340–25.103` · absolute `869.857–874.620`

A compact, progressively built two-route composition—not a map. The upper/secondary route says `FIRST STAY / OTA` and remains intact. The key tag takes the lower gold route labeled `NEXT APPROPRIATE STAY / DIRECT`. The platform route is useful acquisition; the changed consequence is the route of the later appropriate stay.

Snapshot times relative to scene: entry `0.150`, action `2.250`, exit `4.600`.

## Scene 5 — The resolved outcome

`25.103–30.405` · absolute `874.620–879.922`

Hard cut to a separate paper proof bench. The first-booking OTA line remains historical and intact. A measured hotel-owned return line resolves beside the key tag. The abstract object `DIRECT RETURN / VISUAL TEST` appears with the permanent warning `NON-PUBLISHABLE FIXTURE · NOT EVIDENCE`. At word 1846 (`introduction`), the final causal frame is complete and holds for `1.983` seconds through the end of the window.

Snapshot times relative to scene: entry `0.150`, action `3.350`, exit `5.200`.

## Continuity ledger

- Persistent object: one brass key tag with the same rounded-rectangle silhouette, hole, chamfer, and vertical scratch.
- Screen direction: guest/OTA origin left; hotel/operator center; appropriate direct return right.
- Reality-to-system handoff: only the tag contour survives.
- System-to-proof handoff: hard cut; the tag identity and left-to-right ownership direction survive.
- Retired: ineligible/sensitive token at the suppression stop; no later frame revives it.
- Final handoff: locked two-route proof frame held for comprehension; no claim of production approval.
