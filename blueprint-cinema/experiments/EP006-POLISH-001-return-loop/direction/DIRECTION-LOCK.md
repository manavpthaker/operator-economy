# EP006 Polished Control 001 — Direction lock

Status: locked for this isolated internal experiment only. This is not canonical EP006 direction approval and does not advance any EP006 gate.

## Audience inference

An OTA can be useful acquisition for the first stay. Avoiding a repeat acquisition cost on a later appropriate stay requires an ordered, hotel-owned return path: permission first, minimal context second, then qualification, suppression, human judgment, and only then direct routing.

## Word-cued scene contract

| Scene | Relative | Absolute | Locked words | Dominant priority | Exit state |
|---|---:|---:|---|---|---|
| 1 — Return | `0.000–4.375` | `849.517–853.892` | 1775–1787, `Now` through `came` | physical key-tag handoff | tag contour isolated at `x976 y515 w270 h126` |
| 2 — Permission | `4.375–12.240` | `853.892–861.757` | 1788–1805, `through` through `the` | closed permission gate, then memory | `STAY CONTEXT` exists only after permission |
| 3 — Judgment | `12.240–20.340` | `861.757–869.857` | 1806–1823, `visit` through `The` | one decision at a time | ineligible path stopped; eligible route opens after human action |
| 4 — Direct return | `20.340–25.103` | `869.857–874.620` | 1824–1835, `win` through `It's` | appropriate return route | OTA path intact; key tag arrives on direct path |
| 5 — Outcome | `25.103–30.405` | `874.620–879.922` | 1836–1847, `making` through `forever` | abstract, warned proof object | final causal frame holds from word 1846 |

The first 0.146 seconds are an intentional audio handle for word 1775, `Now`; canonical `sequence-economics-05` starts on word 1776, `go`, at 849.663/849.690 depending the plan-unit boundary versus spoken-word onset.

## Scene 1 geometry and action

- Plate: `x0 y0 w1920 h1080`, z0, full opacity.
- Natural vignette crop: no synthetic darkening; the desk runs horizontally through `y420–820`.
- Persistent key-tag focal region: `x976 y515 w270 h126`, z20.
- Guest forearm/hand enters from left plate region `x260 y430 w650 h350`.
- Operator forearm/hand receives from right region `x990 y420 w640 h370`.
- Label `FIRST STAY / OTA`: `x134 y810 w560 h96`, 48px Supreme 500, left aligned, max one line.
- Fixture disclosure: `SYNTHETIC ENVIRONMENT PLATE · INTERNAL TEST`: `x134 y955 w760 h44`, 28px Menlo, left aligned.
- At word 1775 `Now` (`0.000`), cut into the settled desk; no camera move.
- At word 1780 `guest` (`0.907`), the guest hand enters `120px` and slows.
- At word 1785 `first` (`3.080`), the tag touches wood; the key-clack cue is reserved for Resolve.
- At word 1786 `stay` (`3.592`), a 2px Ledger Gold contour traces the tag over `0.500s`.
- At word 1787 `came` (`4.104`), the receiving hand closes around the tag while the camera remains locked.
- Exit: plate cuts away at `4.375`; contour and tag silhouette preserve exact `x976 y515 w270 h126`.

## Scene 2 geometry and action

- Schematic Navy field: `x0 y0 w1920 h1080`, z0.
- Key tag: starts `x976 y515 w270 h126`, then ends `x1006 y515 w270 h126`, z30.
- Closed gate: `x1280 y270 w160 h540`, z25; exact label `PERMISSION` at `x1150 y170 w420 h78`, 52px Supreme 500, centered.
- Memory object: `x1460 y390 w326 h300`, z24; exact label `STAY CONTEXT`, 48px Supreme 500; no additional fields.
- `FIRST STAY / OTA` persists at `x134 y850 w560 h96` until word 1790 ends.
- At word 1788 `through` (`4.390`), tag advances `30px` over `0.620s` and stops against the closed gate without crossing it.
- At word 1790 `OTA` (`4.880`), the historical label gets a single Ledger Gold underline, then becomes secondary.
- At word 1791 `After` (`6.133`), the gate receives a request state but remains closed.
- At word 1796 `permission` (`8.324`), the gate bolt retracts over `0.360s`; Status Sage appears only at completion.
- At word 1800 `touch` (`9.333`), the tag passes the now-open gate.
- At word 1801 `remembers` (`10.584`), the tag silhouette compresses to `32%` scale and moves `160px` upward into a small brass pin at the top of the `STAY CONTEXT` sheet; memory appears only now.
- Exit: `STAY CONTEXT` resolved; no message, profile, or outbound path exists.

## Scene 3 geometry and action

Phase A, relevance (`12.240–15.336`):

- Paper field: `x0 y0 w1920 h1080`.
- Memory object: `x150 y390 w330 h280`.
- Relevance gate: `x630 y350 w390 h360`; label `RELEVANCE + TIMING`, 48px Supreme 500.
- Eligible key tag: `x500 y475 w180 h84` moving right.
- At word 1806 `visit` (`12.280`), memory object enters on a hard cut.
- At word 1812 `reason` (`14.577`), the eligibility route traces to the relevance gate and resolves.

Phase B, suppression (`15.336–16.340`):

- Hard internal cut; Paper + Ink + Suppression Red only.
- Sensitive-case token: `x830 y520 w180 h84`.
- Stop bar: `x1170 y340 w52 h400`; label `SUPPRESSED` at `x1260 y475 w430 h80`, 52px Supreme 500.
- At word 1815 `back` (`15.336`), the token travels to the stop bar over `0.420s`, impacts, and remains visible and motionless.

Phase C, human judgment (`16.340–18.109`):

- Operator plate: `x0 y0 w1920 h1080`, full frame.
- Persistent key-tag focal region in plate: `x650 y560 w300 h140`.
- `HUMAN REVIEW` state: `x1160 y390 w560 h190`, z30; unresolved outline at first.
- At word 1816 `The` (`16.340`), hard cut to operator; the hand is poised above the same tag.
- At word 1818 `booking` (`17.150`), the operator deliberately places the tag into a plain paper review tray. The action is physical, not an interface gesture.
- At word 1819 `takes` (`17.693`), `HUMAN REVIEW` resolves to Status Sage. The outgoing route remains absent through this completion.

Phase D, route authorization (`18.109–20.340`):

- Hard cut back to Schematic Navy.
- Human-review gate: `x360 y390 w480 h260`, resolved.
- Direct route: starts at `x840 y520`, ends at `x1580 y520`.
- Key tag: `x820 y455 w180 h84`, then `x1430 y455 w180 h84`.
- At word 1821 `direct` (`18.109`), and not before, the Ledger Gold route traces and the key tag advances.
- Exit: eligible token continues; suppressed token remains retired at its stop.

## Scene 4 geometry and action

- Schematic Navy field: `x0 y0 w1920 h1080`.
- Hotel anchor/key tag: `x840 y430 w240 h112`.
- Historical OTA route: `x180 y330` to `x840 y470`, 2px Paper at 42% opacity.
- Direct return route: `x960 y560` to `x1710 y720`, 5px Ledger Gold.
- `FIRST STAY / OTA`: `x180 y215 w600 h80`, 48px Supreme 500.
- `NEXT APPROPRIATE STAY / DIRECT`: `x1060 y800 w720 h150`, 48px Supreme 500, max two lines.
- At word 1824 `win` (`20.400`), both route origins are visible but no route is destroyed.
- At word 1825 `isn't` (`20.840`), the OTA line dims only to 42%, never red or crossed out.
- At word 1830 `platform` (`22.022`), `FIRST STAY / OTA` is held as useful historical acquisition.
- At word 1834 `introduction` (`23.148`), the direct return route completes and the tag arrives at the hotel-owned destination.
- Exit: both routes remain; the direct route owns visual priority.

## Scene 5 geometry and action

- Paper proof field: `x0 y0 w1920 h1080`.
- Historical line: `x190 y290 w600 h8`; label `FIRST STAY / OTA` at `x190 y190 w600 h80`.
- Hotel-owned return line: `x790 y590 w820 h10`; label `NEXT APPROPRIATE STAY / DIRECT` at `x790 y700 w850 h120`.
- Key tag: `x1280 y500 w260 h120`, pinned above the direct line.
- Outcome object: `x660 y150 w600 h390`, square Paper-0 with 2px Ink rule.
- Outcome text: `DIRECT RETURN / VISUAL TEST`, 72px Boska 700, centered, max two lines.
- Permanent warning: `NON-PUBLISHABLE FIXTURE · NOT EVIDENCE`, `x670 y570 w580 h50`, 28px Menlo, centered.
- At word 1836 `making` (`25.126`), hard cut to proof; the historic OTA line is already resolved and intact.
- At word 1839 `hotel` (`26.053`), the hotel-owned return line begins tracing in Ledger Gold.
- At word 1843 `buy` (`27.460`), a faint repeat-acquisition loop retracts; it is not destroyed or villain-colored.
- At word 1846 `introduction` (`28.422`), the direct route and abstract outcome resolve; Status Sage replaces Gold only on the tiny resolved-state rule.
- At word 1847 `forever` (`29.250`), the final causal frame is held without new motion.
- Final hold: `28.422–30.405`, `1.983s`.

## Sound cue map reserved for Resolve

| Relative cue | Event | Intent |
|---:|---|---|
| `3.592` | key tag lands | dry brass-on-wood clack, close and natural |
| `4.390` | reality/system handoff | soft paper movement, not a whoosh |
| `8.324` | permission resolves | restrained latch/click |
| `15.336` | sensitive path stops | muted wooden stop, no alarm |
| `18.109` | direct route opens | quiet mechanical activation |
| `28.422` | outcome resolves | soft single chime, no sparkle cascade |

## Observable review checks

1. The same key-tag shape, hole, chamfer, and vertical scratch are recognizable in all five scenes.
2. The memory object is absent before word 1796 completes and appears only after the gate opens.
3. The sensitive token visibly impacts a red stop and never continues.
4. `HUMAN REVIEW` remains unresolved until the operator’s physical action completes.
5. The outbound direct route is absent until word 1821, after human review.
6. `FIRST STAY / OTA` is never destroyed, crossed out, or colored red.
7. The outcome object is visibly labeled as a non-publishable visual test and cannot resemble evidence.
8. No shot exposes a complete node map or more than one dominant priority.
9. The final causal frame is complete by word 1846 and holds approximately two seconds.
10. The small warning and both path labels remain legible in the 480×270 contact sheet.

## Negative constraints

- Do not invent guest data, an interface, a document, a message, a confirmation, a price, a statistic, a result, or a platform identity.
- Do not treat the AI plates as capture, evidence, or asset-ticket fulfillment.
- Do not animate anything merely for energy; no ambient motion.
- Do not show the complete world graph.
- Do not use a camera fly-through, node-map pan, presentation build, or generic kinetic typography.
- Do not claim this experiment as approved production language or footage.
