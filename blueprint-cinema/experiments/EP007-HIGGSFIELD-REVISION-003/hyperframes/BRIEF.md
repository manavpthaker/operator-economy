---
status: selected-for-private-review
workflow: general-video
aspect: 16:9
---

# EP007 · Revision 003

Revise the current one-minute private test so the filmed encounter feels like a natural business conversation and the presenter has subtle head and hand movement. The user said the animation is okay; preserve it byte-for-byte.

Use the locked narration master excerpt [0, 59.86) without rewriting, retiming, processing or replacing its words. The review container remains 1280 × 720, 30 fps, 1,796 frames (59.8666666667 seconds). The last 6.667 ms is picture padding. The presenter passage remains master [48.52, 59.86).

Preserve `compositions/question.html`, `working-model.svg`, its manifest and all four `model-layers` SVGs exactly. Its record window remains 35.08–48.52. Historical approval fields inside the copied model manifest remain unchanged; the current reuse instruction is recorded here and in `STAGING-MANIFEST.json`.

The lead agent's `../DIRECTION-PACKET.md` supplies the revised film direction and cut plan. The composition now wires a shared-review wide shot, a shared-folder insert, an owner-favored two-shot, the unchanged Working Model and a wider presenter. All source offsets are zero. The wrapper file `compositions/buyer.html` remains for continuity, but its composition and video IDs are `shared-folder` and `shared-folder-video`; it no longer represents a buyer single. Media are copied byte-for-byte from this experiment's `../generated` into `assets` for the HyperFrames server.

All three filmed clips are staged and decode correctly. The lead selected avatar retry 205 (job `c3f4b35f-1e27-4457-843b-ad6ca0e9a2a9`) for this private review. Its source is copied byte-for-byte to `assets/avatar-selected.mp4`, SHA-256 `98a9dcdb8f3ccee8c66a5e606459242a77a2dea725a51e999c3fe02135dd9a18`. The lead's review found opening and second-sentence articulation, modest head/hand motion and no obvious sampled anatomy defect. This selection is not owner approval of the new performance.

Generation 201 was rejected because its mouth did not articulate during the first roughly 1.1 seconds of speech. It remains preserved and unreferenced. The filmed takes are selected for the private test with two noted limitations: the owner settles slightly early, and the buyer's soft gaze persists. No old filmed scene video is reused.

The exact opening WAV, identity WAV/FLAC and candidate 03 portrait are staged in `../inputs`. The HyperFrames narration asset is byte-identical to the opening WAV. Prior input and transport manifests, brief and storyboard are retained there with `.baseline` names for provenance only. `STAGING-MANIFEST.json` records all copied hashes and the 13 reverified locked sources.

Keep HyperFrames pinned at 0.8.31. This worker owns local assembly, validation, Studio preview and the high-quality private review render authorized by the current full-edit request. No provider calls, new narration, music, publication, external feedback or gate changes. The lead agent owns the current avatar audio/sync review, independent assembled playback and the run ledger. The prior experiment's spending terms and validation results do not validate new generation.
