diff --git a/site/app/businesses/[slug]/page.tsx b/site/app/businesses/[slug]/page.tsx
index 00e23d74..51bfdbe9 100644
--- a/site/app/businesses/[slug]/page.tsx
+++ b/site/app/businesses/[slug]/page.tsx
@@ -22,6 +22,7 @@ export default async function BusinessPage({ params }: { params: Promise<{ slug:
   const operation = getOperation((await params).slug);
   if (!operation) notFound();
   if (operation.slug === 'direct-booking-recovery') return <DirectBookingPage operation={operation} />;
+  const worksheet = operation.artifact === 'worksheet';
 
   return (
     <div id="top">
@@ -32,7 +33,9 @@ export default async function BusinessPage({ params }: { params: Promise<{ slug:
           <div className="bl-episode-feature__intro">
             <h1 id="business-title">{operation.name}</h1>
             <p>{operation.summary}</p>
-            <p className="oe-page-note">This business was published under the earlier V1 Blueprint format. It is not represented as a V2 Canvas.</p>
+            <p className="oe-page-note">{worksheet
+              ? 'The companion is an Operator Blueprint worksheet: the forms, checklist and modeled arithmetic from the episode. It is not a V2 Canvas.'
+              : 'This business was published under the earlier V1 Blueprint format. It is not represented as a V2 Canvas.'}</p>
           </div>
           <figure className="oe-page-hero__accent" aria-hidden="true"><img src="/illustration/system/operator-loop.svg" alt="" width="480" height="320" /></figure>
         </section>
@@ -40,19 +43,19 @@ export default async function BusinessPage({ params }: { params: Promise<{ slug:
         <section className="oe-section oe-band-inset bl-shell" aria-labelledby="record-title">
           <header className="bl-chapter__head"><p className="bl-chapter__number">Operation №{padOperationNumber(operation.number)}</p><div><h2 id="record-title">The operation and the episode are different titles.</h2><p>The operation names the work you would own. The episode title packages the investigation for video.</p></div></header>
           <div className="bl-ledger-row"><span className="bl-ledger-row__name">Operation</span><p>{operation.name}</p><span className="oe-class">Stable name</span></div>
-          <div className="bl-ledger-row"><span className="bl-ledger-row__name">Buyer</span><p>{operation.audience}</p><span className="oe-class">V1 record</span></div>
-          <div className="bl-ledger-row"><span className="bl-ledger-row__name">Offer</span><p>{operation.offer}</p><span className="oe-class">V1 record</span></div>
+          <div className="bl-ledger-row"><span className="bl-ledger-row__name">Buyer</span><p>{operation.audience}</p><span className="oe-class">{worksheet ? 'Episode record' : 'V1 record'}</span></div>
+          <div className="bl-ledger-row"><span className="bl-ledger-row__name">Offer</span><p>{operation.offer}</p><span className="oe-class">{worksheet ? 'Episode record' : 'V1 record'}</span></div>
           <div className="bl-ledger-row"><span className="bl-ledger-row__name">Episode</span><p>{operation.episodeTitle}</p><span className="oe-class">Editorial title</span></div>
         </section>
 
         <section className="bl-chapter bl-shell" aria-labelledby="watch-title">
-          <header className="bl-chapter__head"><p className="bl-chapter__number">Investigation</p><div><h2 id="watch-title">Watch the episode.</h2><p>The episode carries the original V1 argument and evidence package.</p></div></header>
+          <header className="bl-chapter__head"><p className="bl-chapter__number">Investigation</p><div><h2 id="watch-title">Watch the episode.</h2><p>{worksheet ? 'The episode carries the argument, the evidence and the modeled arithmetic.' : 'The episode carries the original V1 argument and evidence package.'}</p></div></header>
           <div className="oe-media"><a className="oe-video" href={operation.youtubeUrl} target="_blank" rel="noreferrer"><span>Watch №{padOperationNumber(operation.number)} on YouTube</span></a><div><div className="bl-ledger-row"><span className="bl-ledger-row__name">Sources</span><p>{operation.sources} in the published package</p></div><div className="bl-ledger-row"><span className="bl-ledger-row__name">Published</span><p>{operation.published}</p></div></div></div>
         </section>
 
         <section className="bl-chapter oe-band-raised bl-shell" aria-labelledby="download-title">
-          <header className="bl-chapter__head"><p className="bl-chapter__number">Legacy Blueprint</p><div><h2 id="download-title">Download the original working document.</h2><p>The PDF is the historical companion to this V1 episode. It is not a V2 Canvas.</p></div></header>
-          <a className="oe-direct-download" href={operation.pdfUrl}>Download the legacy Blueprint PDF</a>
+          <header className="bl-chapter__head"><p className="bl-chapter__number">{worksheet ? 'Operator Blueprint' : 'Legacy Blueprint'}</p><div><h2 id="download-title">{worksheet ? 'Download the worksheet.' : 'Download the original working document.'}</h2><p>{worksheet ? 'The readiness checklist, the 30-day dependency map, the scope boundary, the stopwatch log and the full model with every assumption shown.' : 'The PDF is the historical companion to this V1 episode. It is not a V2 Canvas.'}</p></div></header>
+          <a className="oe-direct-download" href={operation.pdfUrl}>{worksheet ? 'Download the Operator Blueprint PDF' : 'Download the legacy Blueprint PDF'}</a>
           <p className="oe-page-note">Downloading does not subscribe you to the newsletter.</p>
         </section>
 
diff --git a/site/app/components/BusinessList.tsx b/site/app/components/BusinessList.tsx
index c6d7a291..5af38f00 100644
--- a/site/app/components/BusinessList.tsx
+++ b/site/app/components/BusinessList.tsx
@@ -35,7 +35,7 @@ export function BusinessList({ operations }: { operations: Operation[] }) {
                 <small>
                   {operation.summary}
                   <span className="oe-record">
-                    Episode + Legacy Blueprint · {operation.sources} sources · {operation.published}
+                    Episode + {operation.artifact === 'worksheet' ? 'Operator Blueprint worksheet' : 'Legacy Blueprint'} · {operation.sources} sources · {operation.published}
                   </span>
                 </small>
               </span>
diff --git a/site/app/lib/operations.ts b/site/app/lib/operations.ts
index 28f12b8a..fab4823a 100644
--- a/site/app/lib/operations.ts
+++ b/site/app/lib/operations.ts
@@ -1,4 +1,4 @@
-export type OperationArtifact = 'canvas' | 'legacy_blueprint';
+export type OperationArtifact = 'canvas' | 'worksheet' | 'legacy_blueprint';
 
 export type Operation = {
   number: number;
@@ -16,6 +16,21 @@ export type Operation = {
 };
 
 export const OPERATIONS: Operation[] = [
+  {
+    number: 7,
+    slug: 'exit-readiness-prep',
+    name: 'Sale-Readiness Practice',
+    audience: 'Owner-run businesses',
+    offer: 'Fixed-fee readiness engagement',
+    summary: 'Get owner-run businesses ready to be inspected by a buyer, for a fixed fee that never depends on a sale.',
+    episodeTitle: 'The One-Person Business That Gets Companies Ready for a Sale',
+    // Filled from studio/originate/exit-readiness-prep/launch/links.json after the upload. Never hand-typed.
+    youtubeUrl: '{{EPISODE_URL}}',
+    sources: 4,
+    published: 'Oct 2026',
+    pdfUrl: '/blueprints/exit-readiness-prep.pdf',
+    artifact: 'worksheet',
+  },
   {
     number: 6,
     slug: 'direct-booking-recovery',
@@ -88,6 +103,10 @@ export const OPERATIONS: Operation[] = [
   },
 ];
 
+/** The homepage feature is hand-composed for EP006 (hotel illustration and copy). Keep it pinned
+ *  until a feature is designed for a newer operation; OPERATIONS[0] stays the newest for lists. */
+export const FEATURED_SLUG = 'direct-booking-recovery';
+
 export function getOperation(slug: string): Operation | undefined {
   return OPERATIONS.find((operation) => operation.slug === slug);
 }
diff --git a/site/app/page.tsx b/site/app/page.tsx
index d8776c7f..b2bc6adc 100644
--- a/site/app/page.tsx
+++ b/site/app/page.tsx
@@ -3,7 +3,7 @@ import Image from 'next/image';
 import Link from 'next/link';
 import { NewsletterBand } from './components/NewsletterForm';
 import { SiteFooter, SiteHeader } from './components/SiteChrome';
-import { OPERATIONS, padOperationNumber } from './lib/operations';
+import { FEATURED_SLUG, OPERATIONS, padOperationNumber } from './lib/operations';
 
 export const metadata: Metadata = {
   title: 'The Operator Economy',
@@ -11,7 +11,7 @@ export const metadata: Metadata = {
 };
 
 export default function Home() {
-  const latest = OPERATIONS[0];
+  const latest = OPERATIONS.find((operation) => operation.slug === FEATURED_SLUG) ?? OPERATIONS[0];
   const recent = OPERATIONS.slice(0, 2);
 
   return (
diff --git a/site/data/episodes.json b/site/data/episodes.json
index 4433114c..817313df 100644
--- a/site/data/episodes.json
+++ b/site/data/episodes.json
@@ -143,6 +143,18 @@
       "date": "2026-08",
       "youtube_url": "https://youtu.be/pOoQLaSyUGQ"
     },
+    {
+      "slug": "exit-readiness-prep",
+      "title": "The One-Person Business That Gets Companies Ready for a Sale",
+      "category": "Services",
+      "model": "Projects",
+      "thesis": "A one-person, fixed-fee practice that gets owner-run businesses ready to be inspected by a buyer, years before a sale, and never takes a fee tied to the sale.",
+      "sources_verified": 4,
+      "number": 7,
+      "status": "upcoming",
+      "stage": "scheduled",
+      "expected": "2026-10-05"
+    },
     {
       "number": 5,
       "slug": "small-cohort-business",
