**GapFigure** — the signature before/after economics gap. Gold arrow + gold destination; origin stays neutral. Use for any "enterprise ceiling → solo version" contrast.

```jsx
<GapFigure from="$5.9B" to="$2K" label="The gap" size="xl" onInk />   {/* thumbnail */}
<GapFigure from="$40/mo" to="$2,000" size="md" />
```

One GapFigure per composition — it IS the accent. Sizes sm/md/lg/xl; `onInk` for dark surfaces.
