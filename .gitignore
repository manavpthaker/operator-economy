
# studio working dirs
studio/input/
studio/output/
studio/originate/*/footage_candidates/
studio/originate/*/vo/
studio/remotion/node_modules/
studio/remotion/out/
__pycache__/
.env
.DS_Store

# secrets
.secrets/

# DAW working files (keep local, never in git — >100MB breaks GitHub push)
*.aup4
*.aup4-shm
*.aup4-wal
*.band/
studio/.bedwork/
.vercel
.env*

# episode media — large binaries, regenerable from the pipeline, never in git.
# ep004-final.mp4 alone is 641MB; GitHub hard-fails any file over 100MB.
studio/originate/*/*.mp4
studio/originate/*/avatars/
studio/remotion/public/vo/
studio/remotion/public/thumbs/
studio/remotion/public/footage/
studio/originate/*/footage/
# (site/public/blueprints/*.pdf IS committed — the deployed site serves it)

# python virtualenv (uv-managed, CPython 3.12). Recreate with:
#   uv venv --python 3.12 .venv && uv pip install --python .venv/bin/python -r studio/requirements.txt
.venv/

# simple-icons metadata cache (fetch_logos.py)
studio/.cache/

# scratch + machine-local tooling
tmp/
.agents/
.gtm/

# old takes and cleanup piles in episode folders (local only)
studio/originate/*/_to_delete/
studio/originate/*/vo.pre-*/
studio/originate/*/vo-*-review/
studio/originate/*/resolve_pilot/
studio/remotion/public/coverage/

# blueprint-cinema renders, stills and audio are regenerable; keep scripts, specs and json
blueprint-cinema/**/*.png
blueprint-cinema/**/*.jpg
blueprint-cinema/**/*.jpeg
blueprint-cinema/**/*.webp
blueprint-cinema/**/*.mp4
blueprint-cinema/**/*.mov
blueprint-cinema/**/*.webm
blueprint-cinema/**/*.mp3
blueprint-cinema/**/*.wav
blueprint-cinema/**/*.m4a
blueprint-cinema/**/*.aac
blueprint-cinema/**/*.drp
blueprint-cinema/**/*.woff2
blueprint-cinema/experiments/*/resolve/
blueprint-cinema/**/node_modules/
blueprint-cinema/**/build/
blueprint-cinema/**/*.aiff

# nested episode media under studio/originate and blueprint-cinema (renders, frames, audio,
# provider payloads) — regenerable or stored under ~/Movies/OE; keep scripts, specs, json, md.
studio/originate/**/*.mp4
studio/originate/**/*.mov
studio/originate/**/*.wav
studio/originate/**/*.pcm
studio/originate/**/*.s16le
studio/originate/**/*.bin
studio/originate/**/*.png
studio/originate/**/*.jpg
studio/originate/**/renders/work-*/
!studio/originate/*/launch/*.png
!studio/originate/*/launch/*.jpg
blueprint-cinema/**/*.wav
blueprint-cinema/**/*.pcm
blueprint-cinema/**/*.s16le
blueprint-cinema/**/*.bin

# EP007 4K thumbnail master lives here locally only (8MB); the committed copy is thumbnail.jpg
studio/originate/exit-readiness-prep/launch/thumbnail.png
studio/originate/exit-readiness-prep/launch/thumbnail-source-1344.png
